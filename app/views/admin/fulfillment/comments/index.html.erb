<table class="table table-striped table-bordered">
  <thead>
  <tr>
    <th class='column1_header'>Name</th><th></th><th></th>
  </tr>
  </thead>
  <% for comment in order.comments %>
    <tr>
      <td><%= link_to comment.name, admin_fulfillment_order_comment_path(order, comment) %></td>
      <td><%= link_to "Edit", edit_admin_fulfillment_order_comment_path(order, comment) %></td>
      <td><%= link_to "Destroy", admin_fulfillment_order_comment_path(order, comment), :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>
<%= link_to "New Comment", new_admin_fulfillment_order_comment_path(order), 
  :class => 'btn' %>
