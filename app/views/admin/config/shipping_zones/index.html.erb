<%= link_to 'New Shipping Zone', new_admin_config_shipping_zone_path, :class => 'btn' %>

<table class="table table-striped table-bordered">
  <thead>
  <tr>
    <th class='column1_header'>Name</th>
    <th></th>
  </tr>
  </thead>

<% @shipping_zones.each do |shipping_zone| %>
  <tr>
    <td>
      <%= link_to shipping_zone.name, edit_admin_config_shipping_zone_path(shipping_zone) %>
    </td>
    <td>
      <%= link_to "Edit", edit_admin_config_shipping_zone_path(shipping_zone) %>
    </td>
  </tr>
<% end %>
</table>