= image_tag(@product.featured_image(:large))
- if @product.images.size >= 2
  .row
    - @product.images.drop(1).each do |image|
      .col-md-2= link_to image_tag(image.photo.url(:mini)), '#', :class => 'thumbnail'

    